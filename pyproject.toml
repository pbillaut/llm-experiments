[project]
name = "largelanguagemodel"
version = "0.1.0"
description = "Experimenting with LLMs"
requires-python = ">=3.13"
dependencies = [
    "tiktoken>=0.12.0",
    "torch>=2.11.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.409",
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]
